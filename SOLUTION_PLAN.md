# SOLUTION PLAN — Shipment Status Tracker

**日期：** 2026-08-08
**评审人视角：** 资深货代人（Freight Forwarder）· 软件架构师（Programmer）· 会计（Accountant）· 货代客服/操作（CS & Operations）
**评审方法：** 不是重新翻一遍代码就下结论——而是先核对已有的审查文档（`SYSTEM_COMPLETENESS_AUDIT.md`、`REFERENCE_CRM_ANALYSIS.md`、`TODO.md`、`PRODUCT_BACKLOG.md`）是否仍然成立，再做几项**今天实测**的核查（跑测试、npm audit、读 schema、查 git 状态、查真实配置），最后以四个角色的专业判断给出**排序后的行动计划**。
**范围声明：** 这份文档不重复罗列已有审计的全部细节——那些文档写得很扎实，仍然有效，我会引用而不是复制。这份文档的价值在于：**验证现状 + 跨角色串联 + 给出可执行的优先顺序**。

---

## 0. 结论先行（给没空看全文的你）

**这套系统的"报价 → 订舱 → 操作 → 开票 → 收付款"商业主链条已经做得比大多数小型货代在用的系统更扎实**——税务处理对、金额类型对、审批留痕对、并发锁对。2026-08-02 已经打到 **MVP GA**（8 项 P0 全部完成），今天实测 **306/306 单元测试全绿**。这不是一句空话式的表扬，是我刚刚跑出来的结果。

但有 **5 件事我认为应该在你继续加新功能之前先处理**，理由不是"完美主义"，是因为你现在**用的是真实客户数据、真实金额**，这几件事一旦出问题，代价不是"重跑一次"，是真金白银或合规风险：

| # | 问题 | 谁会中招 | 严重度 |
|---|---|---|---|
| 1 | **P&L 报表不是历史稳定的**——今天算的 3 月利润，8 月补录一条汇率后再算会变 | 你自己做经营决策、将来给会计/税务师看报表 | 🔴 高 |
| 2 | ~~税务记录只有 30 天滚动备份~~ — ✅ **已于 2026-08-08 解决**：新增永久保留的月度归档层 + 一键恢复演练脚本 | — | 已关闭 |
| 3 | ~~CUST-0003 额度设成 0~~ — ✅ **已于 2026-08-08 解决**：客户已从回收站恢复，额度设为 MYR 50,000，信用判定回到 ALLOW | — | 已关闭 |
| 4 | **邮件仍是模拟模式**（未配置真实 SMTP）——报价单、发票、对账单、催款信**都没有真的发给客户** | 所有客户，从系统上线第一天起 | 🔴 高 |
| 5 | ~~`auth` 模块零单元测试~~ — ✅ **已于 2026-08-08 解决**：18 个单元测试，覆盖锁定阈值、锁定过期、账号枚举防护，并经变异检验 | — | 已关闭 |

> **2026-08-08 更新：** 上表 5 项已关闭 4 项，详见 `SECURITY_REVIEW.md` 与 commit `50a1f3e`。
> **核查过程中还发现了一个不在上表、也不在任何扫描报告里的真问题**：这套装着真实客户财务数据的 ERP，此前监听在所有网卡上，在公共 WiFi 下同网段任何设备都能访问，包括登录接口。已修复并实测验证。
> **剩余唯一未决项是 SMTP**——需要你本人配置。

这 5 件事，第 3、4 条**今天就能处理完**（决定一个数字、填几行 `.env`）。第 1、2、5 条工作量都是 **S（半天到一天）**。建议本周内一次性收掉，详见第 7 节。

另外有一个**你需要亲自做的战略判断**（第 6 节）：这套系统现在的实际使用方式，和最早"做成 SaaS 卖给其他货代"的设想，已经在事实上分道扬镳了——现在是**单租户、跑在你自己电脑上、服务你自己一家公司**。这不是缺点，是个诚实的现状，但它决定了接下来该往哪个方向investment。我不会替你做这个决定，但会把两条路分别要付出的代价摆清楚。

---

## 1. 现状核实（今天实测，不是引用旧文档）

| 检查项 | 结果 |
|---|---|
| 后端单元测试 | **306/306 通过**（刚跑，21 个测试套件，57 秒） |
| 后端安全扫描（`npm audit --omit=dev`） | 5 个漏洞：1 高（`brace-expansion` DoS，来自开发工具链）、3 中（`@nestjs/core` 注入类、`exceljs`→`uuid` 缓冲区）、详见第 4.2 节 |
| 前端安全扫描 | 5 个漏洞：**2 高**（`Next.js` DoS/RSC 反序列化、`PostCSS` XSS/任意文件读取）——这两个是**运行时相关**，不是开发工具链噪音，详见第 4.2 节 |
| Git 分支状态 | 当前在 `sprint-05-06-mvp-ga` 分支，**领先 `main` 6 个 commit，尚未合并**——Sprint 05、Sprint 06、以及全部本地化运维加固都还没进 `main` |
| 数据库 | 已从 Docker 迁移到原生 PostgreSQL 17（Windows 服务，端口 5433），`.env` 确认无漂移 |
| `auth` 模块测试 | 确认 **零个** `.spec.ts` 文件 |
| SMTP 配置 | 确认 **未设置**（`.env` 里 `SMTP_HOST` 为空）——邮件仍是模拟模式 |
| `@nestjs/core` 补丁 | 已是 10.x 系列最新版（10.4.22），漏洞修复在 11.x 大版本——**没有免费的小版本修法**，需要评估大版本升级 |
| 多租户模型 | 通读 `schema.prisma` 全部 1236 行，**没有** Organization/Tenant/Company 概念——每张表隐式单租户 |

---

## 2. 四角色评审

### 2.1 资深货代人视角（商业流程是否像真的懂货代的人做的）

**做得对、做得深的地方：**
- 报价单的头部字段（POL/POD、船东、Transit Time、Free Time、Incoterm、SVE 免税标注）是真正懂海运出口报价单长什么样的人设计的，不是通用表单硬套
- SST/SVE 税务处理**正确**：海运运费本来就是 SVE（0% 服务税），系统在报价、发票、贷记单、借记单、AP 账单全部统一走同一套税务引擎，不是各处各写一套——这点很多商用系统都做不到
- Booking → 里程碑（Booked→Gated-in→Loaded→Departed→Arrived→Delivered）→ Job 的链路是 Sprint 06 刚补上的，之前这是最大的缺口（"没有订舱这一步"），现在补齐了
- 供应商比价、KPI 评分（价格/服务/沟通/交期/可靠度/响应速度）、信用管控（硬阻断+审批覆盖）——这些是很多同价位系统没有的深度

**我认为不够、且你现在应该知道的地方：**

1. **提单（HBL/MBL）没有"放单方式"追踪 —— 这是我评审下来认为最大的操作/法律风险**
   系统里 `shipper`/`consignee` 现在只是发票抬头上的自由文本字符串，没有独立的提单实体，更没有 `ORIGINAL / EXPRESS_RELEASE / TELEX_RELEASE` 这种放单方式字段，也没有"正本签发几份、收回几份"的计数器。
   **说白了：系统现在没有任何机制能防止或提醒"正本提单没收齐就放货"——这是货代行业最大的法律风险之一（无单放货）。** 如果你现在自己签发 HBL，这件事完全在系统外靠人工记忆把关。这不是"锦上添花"的功能，是核心单证风控。

2. **没有集装箱实体，算不出滞箱费（D&D）**
   做 FCL 的话，铅封号、进箱/装船/开航/到港/还箱这几个时间点、箱属（SOC/COC/自有/租赁）—— 这些是算滞箱费/滞港费的基础数据，现在完全没有字段承载。

3. **没有实际开航/到港时间（ATD/ATA），只有预计（ETD/ETA）**
   供应商 KPI 评分里"交期表现"这一项，没有实际时间就只能凭感觉打分，没法用数据说话。这是**加一个字段就能解决**的小事，但价值不小。

4. **空运没有计费重（Chargeable Weight）**
   如果你有空运业务，`max(实重, 体积重÷6000)` 这个行业标准算法现在系统里没有实现。

**我的专业判断：** 对"报价到收款"这条商业主链条，这套系统现在已经可以正式用于真实业务（你已经在用了）。但如果你自己签发正本提单、或做 FCL 计算滞箱费，**那部分工作现在还是在系统外靠人处理**——不是系统会算错，是系统压根没有承载这些数据的地方。

---

### 2.2 会计视角（钱和税，经不经得起查）

**做得对的地方（这些是我特意找茬也没找出问题的）：**
- 金额全部用 `Decimal(14,2)`，没有一处用浮点数——这是很多系统埋雷的地方，这里没埋
- 单据编号（发票号、报价号、订舱号、贷记单号）全部由带行锁的 `SequenceService` 生成，前端传不了、并发也不会撞号
- `totalAmount`/`taxAmt`/`amountPaid`/`grossProfit` 这些金额字段**全部服务端计算，前端传值一律忽略**——防篡改做得很干净
- AP（应付）那边的付款冲销做得很规范：行锁、幂等保护（`reversedAt`）、强制填冲销原因、完整审计——这是我见过的中小型系统里少见的严谨度
- 审批阈值、信用管控、每一笔金钱操作都有审计日志（谁、什么时候、为什么）

**我作为会计会挑出来的问题，按严重度排：**

1. 🔴 **P&L 报表不是历史稳定的（`TODO.md` 里已经记录，我确认仍未修复）**
   `PnlService` 用的是"最新汇率"换算历史期间的收入/成本，不是"当时的汇率"。这意味着：**你今天跑一份 3 月份的损益表，8 月补录一条新汇率之后再跑同一份 3 月报表，数字会变。** 这不是四舍五入的误差，Sprint 03A 验证时实测过一次数字直接跳动了 `180 × (9.99 − 4.45)`。
   对会计来说这是不能接受的：**报表必须是"在那个时点看，永远是那个数字"**，否则你没法向自己、向税务师、向银行解释"为什么这个月的利润和上次给你的不一样"。
   好消息是**修法已经现成**：系统已经有 `FxService.historicalConverter()`（按单据日期取当时汇率），只是 P&L 计算没用它。工作量是 **S**，我建议这是继续加新功能之前**第一件要修的事**。

2. 🔴 **税务记录留存期不够——30 天备份 vs 法定 7 年**
   系统现在的备份是"每天存一份，只留最近 30 份"。这个设计目的是防灾（硬盘坏了怎么办），**但马来西亚 SST（服务税/销售税）的法定记录留存期是 7 年**（Sales Tax Act 2018 / Service Tax Act 2018 及相关规定）。30 天滚动备份**不满足这个要求**——如果稽查要看两年前的记录，而那份日常备份早被滚动删除了，OneDrive 上大概率也早就同步覆盖了。
   **建议**：在现有每日备份之外，**另外做一份"年度/季度归档"，永久保留、不进 30 天滚动**（例如每年 1 月做一次前一年度的完整导出，存到一个不会被自动清理的位置）。工作量：**S**，用现成的 `scripts/backup-db.js` 稍改一下存放策略即可。

3. 🔴 **CUST-0003 额度=0，这个客户现在开不出任何发票**
   Sprint 04 上线信用管控时就发现了这个数据，写进了 `TODO.md`，**到今天还没有人决定**。系统的设计是"额度算不出来就拒绝"（fail-closed），额度设成 0 和"没有额度"在系统里含义完全不同——0 是"一分钱都不能欠"，NULL 才是"不设限"。
   这不是代码问题，是**等你一个业务决定**：这个客户到底要不要给额度？给多少？还是维持 0（也就是这个客户必须每单预付/COD）？

4. 🟠 **AR 收款不能冲销，AP 可以**
   收错客户的钱、录错金额，现在唯一办法是**作废发票重开**——这会打断发票号的连续性。对 SST 申报来说，发票号跳号需要能解释清楚原因，作废重开次数多了会让凭证链变得难看。AP 那边已经有完整的冲销模式（行锁+幂等+审计），**照搬过来给 AR 用**，工作量 M，不算大。

5. 🟡 **没有对接真正的账本（Xero/QuickBooks），也没有总账（GL）**
   现状是：AR、AP、CN/DN 构成了干净的子账，可以导出给会计师做账，**但这套系统本身不是法定的会计账簿**。这不是这个阶段该做的事（自建 GL 工作量是 XL，接现成的记账软件便宜十倍），但**我建议你明确告诉你的会计/报税代理**："这是业务系统，不是账本，月结/年结数据需要你从这里导出后录入正式的账"——避免以后有人误以为这套系统的数字可以直接拿去报税。

---

### 2.3 程序员/架构师视角（代码经不经得起长期维护和真实流量）

**架构层面做得好的地方：**
- "一个财务数值只能有一个计算源头"这个纪律被显式执行且有文档记录（例如 `issuedNoteNetMap` 同时服务 AR 账龄、收款、信用敞口三处，不是三份各自实现）
- 状态机（报价、发票、订舱、里程碑、AP 账单）全部**只能往前走**，非法跳转在类型层面就会被拦，不是运行时才发现
- 并发场景下的行锁（`FOR UPDATE`）用得对，而且是**集成测试真的跑出过一次真实的竞态 bug**（AP 付款冲销的重复提交），修复后有回归测试守着——这说明测试不是摆设，是真的抓到过东西
- 权限码是 TypeScript 联合类型，权限码打错字是**编译错误**，不是运行时才发现的静默失效
- 306 单元 + 33 集成 + 7 浏览器测试，CI 有独立的 job 跑集成测试和浏览器测试

**我这次实测发现的问题：**

1. 🟠 **`brace-expansion`（高危）、`@nestjs/core`（中危注入）、前端 `Next.js`（高危 DoS/RSC 反序列化）、`PostCSS`（高危 XSS/任意文件读取）—— npm audit 全部列出**
   `brace-expansion` 是开发工具链（eslint/jest 用的 glob 库）带进来的，**运行时基本不会被外部触发**，优先级低。
   但 **`Next.js` 和 `PostCSS` 是前端运行时依赖，这是一个真实对外服务的网页应用**，这两个高危漏洞需要认真评估，不能当噪音忽略。`@nestjs/core` 的修复在 11.x 大版本里，10.x 系列没有免费的补丁号可以直接吃——这意味着要修就要动大版本升级（见下一条）。
   **建议**：本周内针对 Next.js 的两个具体 CVE（Image Optimizer remotePatterns、RSC 反序列化）逐条确认系统是否命中触发条件（有没有用 `next/image` 远程图片、有没有暴露 RSC 端点给不受信输入），如果命中就是真实风险，如果没命中就明确记录"已评估，不适用"，别让它只是挂在 audit 报告里没人看。

2. 🟠 **`auth` 模块零单元测试**
   `Glob` 确认整个 `auth` 目录（登录、密码校验、账号锁定 `failedLoginAttempts`/`lockedUntil`、JWT 签发）**没有一个 `.spec.ts` 文件**。集成测试会走登录这条路径，但"失败 5 次锁定账号"这种边界逻辑，没有被直接单测覆盖过。**这是全系统唯一的安全边界**，现在你的系统里存的是真实客户的财务数据，这个测试缺口的优先级应该提到最前面。工作量 S–M。

3. 🟡 **`sprint-05-06-mvp-ga` 分支落后 `main` 6 个 commit 未合并**
   Sprint 05（AR 逾期自动化）、Sprint 06（订舱+里程碑，也就是 MVP GA 本身）、以及最近的本地化运维加固（备份、清demo数据）**全部还没进 `main`**。如果 `main` 是你认为的"权威/可部署"分支（比如以后要部署到 Render 云端），现在从 `main` 部署会得到一个缺了两个 sprint 的旧版本。**建议尽快合并**，现在测试全绿、状态稳定，是合并的好时机。

4. 🟡 **技术栈落后一个大版本**：NestJS 10（现行 11）、Next.js 14（现行 16）、React 18（现行 19）
   都还没到停止维护的地步，不紧急，但**缺口只会越来越大**——现在是跨一个大版本，拖得越久将来要跨的版本越多，回归测试的工作量也越大。`PRODUCT_BACKLOG.md` 里的 T-5 已经记录了这件事，建议给它一个具体的时间窗口（比如"未来 2-3 个月内找一个相对空闲的 sprint 做"），而不是无限期挂着。

5. **多租户：完全没有，这是刻意的，不是遗漏**——这一点我放到第 6 节战略判断里讲，因为它牵涉到一个业务方向决定，不是单纯的技术债。

---

### 2.4 货代客服/操作视角（一线的人每天用起来顺不顺手）

**做得顺手的地方：**
- 信用状态不是藏在某个报表里，而是直接出现在客户列表、发票开立弹窗、AR 账龄表上——客服不用切换好几个页面去确认"这个客户还能不能接单"
- 通知中心覆盖报价到期、供应商价格到期、工单延误、低毛利、高成本、付款到期，而且有去重键，不会同一件事天天弹提醒轰炸
- 订舱页面的 SI/VGM/CY 截关日期会变色提醒——视觉上是对的

**我作为一线操作/客服，日常会撞到的墙：**

1. 🔴 **邮件是模拟的，客户其实什么都没收到**
   这是我核实出来、我认为**最紧急的一条**。报价单、发票、对账单、催款信——系统界面上写着"composed and logged"，**但没有 `SMTP_HOST` 配置，等于一封都没真的发出去**。如果你以为客户已经收到对账单在等付款，而实际上邮件从没发出去过，这直接影响回款。这条**今天就能解决**，`LOCAL_OPERATIONS.md` 第 3 节有现成的配置步骤（Gmail 应用专用密码，5 分钟搞定）。

2. 🟠 **客户/供应商资料不能上传文件，只能贴链接**
   营业执照、税务登记证、信用申请表这些——新客户 onboarding 时客服只能贴一个外部链接（比如 Google Drive），文件本身不在系统里。工单文档已经有完整的上传+存储基础设施，这是复用不是新建，工作量 M。

3. 🟠 **订舱单、工单、对账单没有 PDF**
   订舱单要发给船公司确认，现在只能截图；对账单客户要存档版本，给不了 PDF。打印模板机制是现成的（报价单、发票都有），套用即可，工作量 M。

4. 🟡 **截关日期只有变色提醒，没有主动推送**
   操作人员如果没有正好开着订舱页面，会错过一个变红的截关日期。接进现成的通知扫描机制里就行（`NotificationsService.scan()` 已经有这个模式，Sprint 05 处理逾期发票用的就是它），工作量 S。

---

## 3. 跨角色交叉发现（多个视角同时命中，优先级应该更高）

这些是"不止一个专业角色会关心"的问题，代表它们的实际影响面比看起来更大：

| 问题 | 命中的角色 | 为什么更紧急 |
|---|---|---|
| P&L 历史不稳定 | 会计 + 程序员 | 会计要求数字稳定，程序员已经有现成修法没用上 |
| 无单放货风险（HBL 放单方式缺失） | 货代 + 会计 | 既是操作合规风险，出问题也是实打实的赔偿责任 |
| CUST-0003 额度=0 | 会计 + 客服 | 一边是数据卫生问题，一边是真实客户今天就被卡住 |
| 邮件仍是模拟模式 | 客服 + 会计 | 催款信发不出去直接影响回款，两个角色都受影响 |
| `auth` 零测试 | 程序员 + （隐含）会计 | 现在守护的是真实财务数据的安全边界 |
| 7 年留存 vs 30 天备份 | 会计 + 程序员 | 合规要求 + 备份机制都要改，谁都不能单独解决 |

---

## 4. 附：本次实测的技术细节

### 4.1 测试与构建

```
backend  npm test        → 21 suites, 306 tests, 全部通过 (57.2s)
```

### 4.2 npm audit 完整清单

**后端：**
| 严重度 | 包 | 问题 |
|---|---|---|
| 高 | `brace-expansion` | ReDoS（开发工具链依赖，运行时暴露面低） |
| 中 | `@nestjs/core` / `@nestjs/platform-express` | 输出中特殊字符处理不当（注入类），修复在 11.x |
| 中 | `exceljs` → `uuid` | uuid v3/v5/v6 buffer 越界检查缺失 |

**前端：**
| 严重度 | 包 | 问题 |
|---|---|---|
| 高 | `Next.js` | Image Optimizer remotePatterns DoS；RSC 反序列化 DoS |
| 高 | `PostCSS` | `</style>` 未转义 XSS；sourceMappingURL 任意文件读取 |
| 中 | `brace-expansion`、`exceljs`→`uuid` | 同上 |

### 4.3 关键版本快照

| 组件 | 当前 | 最新大版本 |
|---|---|---|
| `@nestjs/core` | 10.4.22（10.x 系列最新） | 11.1.28 |
| `next` | 14.2.20 | 16.3.0 |
| `react` | 18.3.1 | 19.x |
| `@prisma/client` | 5.22.0 | — |

### 4.4 Git 状态

- 当前分支：`sprint-05-06-mvp-ga`
- 相对 `main`：领先 6 commit，落后 0 commit
- 最新 commit：`052728c chore: make the system dependable to run locally day to day`

---

## 5. 战略判断：这套系统现在到底是什么？（需要你决定，我不替你决定）

从代码和文档看，这套系统的**实际使用方式**已经和最初的设想产生了分歧：

- **`schema.prisma` 里没有任何 Organization/Tenant 概念**——每一张表都是隐式单租户
- **`LOCAL_OPERATIONS.md` 明确写着**"在这台电脑上，用这套系统跑真实的货代业务"——单机、单公司
- **Cloudflare R2（云存储切换）被明确判定为"本地运营不需要"**——因为本地磁盘不会像 Render 那样每次部署就清空
- 数据库已经从"为云部署准备的 Docker"迁移到"这台电脑上的原生 Windows 服务"

这些都指向一个方向：**现在这套系统的实际定位是"Golden Freight（你自己的公司）的内部 ERP"，不是"要卖给其他货代公司的 SaaS 产品"。**

这两条路后续要投入的东西完全不同：

| | **继续走"自用 ERP"** | **回到"对外销售 SaaS"** |
|---|---|---|
| 多租户改造 | 不需要 | **必须做，且是 XL 工作量**——现在几乎每张表都要加租户隔离 |
| 云部署+计费系统 | 不需要 | 必须做 |
| 安全隔离等级 | 现在的单租户强度足够 | 需要租户间数据隔离审计、更高安全标准 |
| 下一步该做什么 | 深化你自己的操作能力：HBL、集装箱、Portal 给你自己的客户用 | 先做租户隔离，再谈功能 |
| 现有 8 个 Sprint 的投入 | 完全不浪费，直接受益 | 大部分商业逻辑仍然复用，但架构层要重做 |

**我的建议**：如果你现在满意于"这是我自己公司在用的系统，好用、省钱、比 Excel 强太多"，那就**不要**现在启动多租户改造——这是纯粹的沉没成本陷阱，你会为了一个还没确定要不要做的生意，先牺牲半年时间做架构，而不是把这半年花在让你自己的操作更顺（HBL、集装箱、给你自己客户用的 Portal）。等真的有别的货代公司明确表示要付费用这套系统时，再启动多租户——那时候的需求会更清楚，改造范围也能更准确评估。

---

## 6. 建议行动顺序

### 立即（本周内，合计约 1-2 天工作量）

| 优先级 | 事项 | 工作量 | 谁来定 |
|---|---|---|---|
| ~~1~~ | ~~决定 CUST-0003 的信用额度~~ — ✅ 已完成（恢复客户 + 额度 50,000） | — | 已完成 |
| **1** | **配置真实 SMTP，确认邮件真的发出去** | 30 分钟 | **你（唯一剩余项）** |
| ~~3~~ | ~~合并 `sprint-05-06-mvp-ga` 进 `main`~~ — ✅ 已完成（快进 7 commit） | — | 已完成 |
| ~~4~~ | ~~年度归档备份~~ — ✅ 已完成（月度归档 + `verify-backup.js` 演练脚本） | — | 已完成 |
| ~~5~~ | ~~`auth` 模块补单元测试~~ — ✅ 已完成（18 测试，经变异检验） | — | 已完成 |
| ~~6~~ | ~~Next.js CVE 逐条核实~~ — ✅ 已完成，见 `SECURITY_REVIEW.md`；**过程中发现并修复了局域网暴露问题** | — | 已完成 |

### 近期（2-4 周）

| 事项 | 工作量 | 解决什么 |
|---|---|---|
| 汇率快照钉在单据上（修 P&L 历史不稳定） | S | 会计报表可信度 |
| AR 收款冲销（照搬 AP 模式） | M | 收款纠错不再需要作废重开 |
| 客户/供应商文档上传 | M | onboarding 体验 |
| 订舱单 + 对账单 PDF | M | 对外交付物 |
| 截关日期主动推送 | S | 操作不再靠人盯屏幕 |

### 中期（1-3 个月，对应 `IMPLEMENTATION_ROADMAP.md` Phase 3）

- **P1-5 结构化收发通知方（shipper/consignee/notify）**——是提单、集装箱、报关的共同地基，建议排在前面
- **P1-3 提单 HBL/MBL，含放单方式追踪**——本次评审认定的最高操作/法律风险项
- **P1-6 集装箱实体 + D&D**——做 FCL 的刚需
- **P1-1 会计系统对接**（Xero/QuickBooks）或至少做定期结构化导出
- NestJS/Next.js 大版本升级——找一个相对空闲的窗口

### 战略（等你对第 5 节做出判断之后再排期）

- 如果确定要对外销售：多租户重构立项，作为独立项目，不是现有 sprint 节奏的延续
- 如果确定只做自用：按上面的中期计划继续深化，Customer Portal（P1-2）的目标读者是"你自己的客户"，不是"卖点"

---

## 7. 一句话总结

**核心商业链条扎实，测试和纪律性在同价位系统里少见，MVP GA 是实打实达成的，不是自我宣称。** 但现在系统里流转的是真金白银和真实客户，所以"P&L 数字要稳、税务记录要留够年限、邮件要真的发出去、安全边界要有测试"这几件事的优先级应该高于任何新功能——它们不是锦上添花，是继续放心把生意交给这套系统的前提。这些加起来大约一周就能收掉，收掉之后再谈集装箱、提单、会计系统对接这些更大的项目，会踏实很多。
